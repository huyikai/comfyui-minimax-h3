---
description: 用户说「出成片」时的自动化流程、自检清单与迭代纪律
alwaysApply: true
---

# 出成片：不要问，自己做到底

用户说「出成片 / 做到成片为止」＝ 授权把整条链路跑完：**precheck → 0.4 试片 → 自检 → 修 → 0.9 → 自检 → 邮件**。中途不要回来征求意见、不要停下等确认。唯一例外是要改故事走向本身。

## 迭代纪律（最要紧的一条）

**发现问题不许直接重跑。** 先定位是哪一层出的问题、把原因改掉，再跑：

| 症状 | 先查哪里 |
| --- | --- |
| 入画人数不对 | 该 clip 的 Identity lock 写了几个人，空位是不是写成了实物 |
| 说话的人嘴不动 | `<d>` 有没有挂在这个人身上、这一拍是不是被写成了旁观 |
| 演的不是那件事 | 中文对照的节拍是不是没写满、英文是不是只剩摘要 |
| 有语音无字幕 | 中文对照里这句的起止秒数缺没缺 |
| 字幕/黄字位置不对 | `run_video_scripts.py` 的 ASS 样式，不是脚本问题 |
| 画面崩坏、构图不可控 | 确实无从下手时才换 seed，并在履历里写明「已排除脚本原因」 |

**先改对照，再回写英文。** 同一条 clip 最多迭代 **5 次**，尽量修到过。

每一轮都追加到 `logs/qc-history/<账号>-<NN-作品>.md`：第几轮、哪一条、什么现象、判断原因、改了什么、结果。仍然过不了就留在履历里，继续往下走，并在完成邮件里列出来。

**不许**为了过检把对白删掉、把段跳过、把时长改短、把问题 clip 换成别的镜头。

## 自检清单

**只要不符合预期就要处理**，下面是至少要看的，不是全部。

脚本算的，看日志：

1. `[QC]` 音画时长差、响度 / 真峰值 / LRA、与参考片的响度差 < 1.5 dB、接缝空洞与底噪台阶
2. `[QC-WARN]` 逐条判断。「有话没字幕」出现在无对白的 clip 上属正常，其余都要处理
3. `ffprobe` 成片：时长 ≈ 各 clip 之和 + 片尾，分辨率 / 帧率正常，音轨在

抽帧读图，每段至少 1 帧 + 片尾 1 帧：

```powershell
.\.venv\Scripts\python.exe tools\qc_frames.py 成片.mp4 --work "人间隙/04-懦弱" --every 12 --tail
```

4. 入画人数与该 clip 的 `Identity lock` 一致
5. 有对白的拍里，说话的人嘴在动
6. 黄字只在每个曾经／后来块开头约 2.5 秒、贴左上；同一块后半段不应再出现
7. 白字在底部，不压脸、不出画、不与黄字重叠
8. 片尾字卡各行对齐、行尾没有逗号
9. 画面没崩：手指、肢体、道具上的乱码字
10. 跨段没有把同一张脸用在两个不同角色上
