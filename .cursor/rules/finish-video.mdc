---
description: 用户说「出成片」时的自动化流程、自检清单与迭代纪律
alwaysApply: true
---

# 出成片：不要问，自己做到底

用户说「出成片 / 做到成片为止」＝ 授权把整条链路跑完：**precheck → 0.4 分片 → 逐段自检修到过 → 合剪 → 合剪自检 → 0.9 直接合剪 → 合剪自检 → 封面 → 邮件**。中途不要回来征求意见、不要停下等确认。故事按现稿走：执行错误自己修，要动走向才能圆的记进履历、按现稿继续跑。

**先修分片，再看合剪。** 一条 clip 崩了就整条重合剪、整条重看，是把渲染时间和读图反复浪费在同一个问题上。0.4 生成时加 `--no-concat` 渲完就停；尽量把每条修到过，再 `--concat-only` 拼。单条 5 次仍不过：记进履历，**照样合剪、照样出 0.9**，不要为一条卡死整条链。

**逐段自检只在 0.4 做。** 0.9 是同一批脚本同一个 seed，分片级那四项在 0.4 就定死了，重读一遍白花时间。0.9 渲完直接合剪，只走合剪级；合剪抽帧本来就要求每段至少 1 帧，够兜住「某段在高分辨率下崩了」。

## 迭代纪律（最要紧的一条）

**发现问题不许直接重跑。** 先定位是哪一层出的问题、把原因改掉，再跑：

| 症状 | 先查哪里 |
| --- | --- |
| 入画人数不对 | 该 clip 的 Identity lock 写了几个人，空位是不是写成了实物 |
| 说话的人嘴不动 | `<d>` 有没有挂在这个人身上、这一拍是不是被写成了旁观 |
| 演的不是那件事 | 中文对照的节拍是不是没写满、英文是不是只剩摘要 |
| 说话的人不该说这句话 | 该 clip 的 `Speaking assignment`，以及段总表时间轴里这一拍标的说话人 |
| 有语音无字幕 | 中文对照里这句的起止秒数缺没缺 |
| 字幕/黄字位置不对 | `run_video_scripts.py` 的 ASS 样式，不是脚本问题 |
| 画面崩坏、构图不可控 | 确实无从下手时才换 seed，并在履历里写明「已排除脚本原因」 |

**先改对照，再回写英文。** 同一条 clip 最多迭代 **5 次**，尽量修到过。

每一轮都追加到 `logs/qc-history/<账号>-<NN-作品>.md`：第几轮、哪一条、什么现象、判断原因、改了什么、结果。仍然过不了就留在履历里，继续往下走。程序合剪成功那封邮件**不改**；遗留由 agent 另发一封，开头就是未过检条目。

**不许**为了过检把对白删掉、把段跳过、把时长改短、把问题 clip 换成别的镜头。

## 自检清单

**只要不符合预期就要处理**，下面是至少要看的，不是全部。

### 脚本级：生成之前就该抓到的

先跑 `precheck`。它能机械判定的都替你判了：锁句跨条不一致、节拍没从 0 铺满到时长、同一句台词出现在两条、段总表对白与 clip 对不上、有 `<d>` 却没写 `Speaking assignment`。这些**不需要理解剧情**，`[FAIL]` 必改，`[WARN]` 逐条判断。

再逐句核 `[SPEAK]` 表——它把每句 `<d>` 和它挂在谁身上并排列出来。**问「这句话该由谁说」**：

- **称谓**：`妈妈`、`爸爸`、`老师` —— 喊人的那个是晚辈 / 下级，不是被喊的人
- **通报的信息**：`孩子找到了` 是搜救方说的，不是被救方
- **祈使**：`别去了` 是劝阻方说的，不是要去的那个人
- **人在不在画面**：说话人不入画时，写成 `a … voice (S1), off-screen`，并让画面上那个人 `has no speaking task and keeps his mouth closed`

这一层**必须在生成前过**。挂错了人，画面会忠实地把台词演到错的那张嘴上，分片自检的「说话的人嘴在动」照样通过——检查项和错误正交，抓不到。等看出不对已经烧掉一轮渲染。

### 故事逻辑审读：机器判不了的那部分

`precheck` 只认格式，认不出「三十岁的人喊妈妈」为什么荒谬。按段起子 agent 通读一遍（段总表 + 该段所有 clip），至少看七件事：

1. **说话人**：台词内容限定了谁能说（称谓、通报的信息、祈使对象、评价的方向）
2. **因果链**：上一条的结果是不是下一条的前提。听见喊声 → 才找到孩子；没有那声，下一条的「找到了」就没来由
3. **能力与年龄**：这个人做得到这件事吗
4. **道具与外观连续**：这一条出现的东西，上一条哪来的；同一个人衣服变没变
5. **场景与时间**：过去 / 现在的切换有没有交代，同一场景的光线天气对不对得上
6. **台词不与动作矛盾**：说「我不敢」的人不能同时主动往下跳
7. **对得上戏剧核**：这一段演的是不是段总表写的那句

**子 agent 只报疑点和建议，不许改文件。** 报回来的每一条，主 agent 自己读过脚本再动手。

### 改还是记：看会不会改变故事的意思

审读出来的东西分两类，**处理方式不同**；两类都不许停下来问：

| 类别 | 判断标准 | 怎么办 |
| --- | --- | --- |
| **执行错误** | 改完这一段讲的还是同一件事 | 直接改，写进履历。例：说话人挂错、锁句不一致、节拍没铺满、总表与 clip 对白不一致、画外音被塞给画面里的人 |
| **故事漏洞** | 要加一拍、改人物关系、改动机才能圆 | **不改、不停**。记进履历，按现稿往下跑，遗留邮件列出 |

「出成片」授权的是把现稿做成片，不是改故事。**不许**为了让逻辑自洽而删台词、跳段、缩时长、换镜头，也不许为此中断链路来问。

### 分片级：单条自己就能判的

每条 clip 抽三帧拼一张横条，一条一图地读：

```powershell
.\.venv\Scripts\python.exe tools\qc_frames.py --work "人间隙/04-懦弱" `
  --dir "$HOME\Downloads\人间隙-04-懦弱-0.4"
```

1. 入画人数与该 clip 的 `Identity lock` 一致
2. 有对白的拍里，说话的人嘴在动
3. 演的是那件事：对着中文对照的节拍看，不是看着「像那么回事」就算过
4. 画面没崩：手指、肢体、道具上的乱码字

横条上判不准就对这一条 `--at` 抽全分辨率的帧细看。尽量修到这四条都过；**5 次仍不过就合剪出片**，遗留另发邮件，不要停下来问。

条数多时把读图拆给子 agent 并行筛，**子 agent 只回报现象、不定夺**，被标出来的那几张主 agent 亲自看过再动手。

### 合剪级：拼起来才看得出的

脚本算的，看日志：

5. `[QC]` 音画时长差、响度 / 真峰值 / LRA、与参考片的响度差 < 1.5 dB、接缝空洞与底噪台阶
6. `[QC-WARN]` 逐条判断。「有话没字幕」出现在无对白的 clip 上属正常，其余都要处理
7. `ffprobe` 成片：时长 ≈ 各 clip 之和 + 片尾，分辨率 / 帧率正常，音轨在

抽帧读图，每段至少 1 帧 + 片尾 1 帧：

```powershell
.\.venv\Scripts\python.exe tools\qc_frames.py 成片.mp4 --work "人间隙/04-懦弱" --every 12 --tail
```

8. 黄字只在每个曾经／后来块开头约 2.5 秒、贴左上；同一块后半段不应再出现
9. 白字在底部，不压脸、不出画、不与黄字重叠
10. 片尾字卡各行对齐、行尾没有逗号
11. 跨段没有把同一张脸用在两个不同角色上

5–10 改的是 `run_video_scripts.py` 或段总表，`--concat-only` 重合剪即可，**不要重生成分片**。只有 11 和确实是画面的问题才退回分片级重跑那一条。
