---
description: 用户说「出成片」时的自动化流程与合剪后自检清单
alwaysApply: true
---

# 出成片：不要问，自己做到底

用户说「出成片 / 做到成片为止」＝ 授权把整条链路跑完：**0.4 试片 → 自检 → 修 → 0.9 → 自检 → 邮件**。中途不要回来征求意见、不要停下等确认。唯一例外是要改故事走向本身。

## 每次合剪后必做

1. `ffprobe` 成片：时长 ≈ 各 clip 之和 + 片尾，分辨率 / 帧率正常，音轨在
2. 逐条看日志里的 `[QC-WARN]`。「有话没字幕」出现在无对白的 clip 上属正常，其余都要处理
3. 抽帧读图（每段至少 1 帧 + 片尾 1 帧）：

```powershell
ffmpeg -y -ss 12.5 -i out.mp4 -frames:v 1 chk.jpg
```

   - 黄字只在每个曾经／后来块开头约 2.5 秒、贴左上；同一块后半段不应再出现。白字在底部。两者不重叠、不出画
   - 入画人数与该 clip 的 `Identity lock` 一致
   - 有对白的拍里，说话的人嘴在动
   - 片尾字卡各行对齐、行尾没有逗号
4. 音频：`[QC]` 行里成片与参考片的 LUFS 差 < 1.5 dB

## 修不动怎么办

同一个 clip 最多重跑 2 次（先改该 clip 的中文对照、再回写英文，或换 seed）。仍不行就追加到 `logs\qc-open.md`：哪一条、什么问题、试过什么，然后继续往下走，并在完成邮件里列出来。

**不许**为了过检把对白删掉、把段跳过、把时长改短。
