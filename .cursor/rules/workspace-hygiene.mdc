---
description: 中间物一律进 .scratch，不许在仓库根散落 tmp_* 文件
alwaysApply: true
---

# 不要把仓库根当草稿纸

读片、质检会产生大量抽帧和裁切块。它们**全部**写进 `.scratch/<账号>-<NN-作品>/`（已 gitignore），作品收工后整个目录可以删。

```text
.scratch/人间隙-04-懦弱/
  probe.json  scene.txt  ocr.txt
  frames/  qc/  source/
```

## 硬性约束

- 仓库根**不许**出现 `tmp_*.py`、`tmp_*.txt` 或 `tmp_*/`。要跑一次性分析，用下面的工具；工具不够用就**改工具**，不要另存一个 `xxx2.py`
- `logs/` 只放运行状态：`video_script_progress.json`、`video_script_run.log`、`smtp.json`、`reference.json`、`run.lock`、`batch-status.json`、`cookies.txt`（yt-dlp 用，已随 `logs/` gitignore）。抽帧、音频片段不许往里塞
- 源片已经在 `video-script\<账号>\<NN-作品>\原片.mp4`，不要再往本仓库拷一份

## 工具

| 命令 | 干什么 |
| --- | --- |
| `tools\envcheck.py --url <链接> --mail` | 开跑前查抖音 cookie（Chrome + Cursor 内置浏览器），失败发邮件 |
| `tools\see_image.py --strip <横条>` | `Read` 读图失败时的 SDK 兜底 |
| `tools\read_source.py <片> --work 人间隙/NN-名 --ocr` | 只做机械抽帧/硬切/OCR（默认 1 秒）。写稿读片请用下一行 |
| `tools\source_agent.py <片> --work 人间隙/NN-名` | 0.5 秒抽帧 + OCR + 转写 + SDK 按窗读图，出 `source/timeline.json` |
| `tools\qc_frames.py --dir <分片目录> --work 人间隙/NN-名` | 每条 clip 抽三帧拼一张横条，逐段自检用 |
| `tools\qc_frames.py <成片> --work 人间隙/NN-名 --at 1.2,4 --tail` | 成片抽检帧 |
| `tools\qc_agent.py --work 人间隙/NN-名` | 分片横条的 SDK 初筛 |

```powershell
    .\.venv\Scripts\python.exe tools\source_agent.py 原片.mp4 --work "人间隙/NN-名"
```

## 结论落库，过程不落库

切点、对白、屏幕字时间线这些**结论**要写进 video-script 那边的 `00-overview.md`。帧、横条、OCR/ASR 缓存是过程，随时能从 `原片.mp4` 重算，不要留、更不要提交。写稿以 `source/timeline.json` 为读片结论，不要通读 `frames/`。
